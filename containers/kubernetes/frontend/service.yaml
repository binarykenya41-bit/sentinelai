apiVersion: v1
kind: Service
metadata:
  name: sentinel-frontend
  namespace: sentinelai
spec:
  selector:
    app: sentinel-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
